# Copy to `.env` and fill in real values. `.env` is gitignored.

# Only needed if you switch the LLM or embeddings to OpenAI.
# OPENAI_API_KEY=sk-...

# Override the default Ollama host (default is http://localhost:11434).
# OLLAMA_HOST=http://localhost:11434
