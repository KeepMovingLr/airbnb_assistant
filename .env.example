# Copy to `.env` and fill in real values. `.env` is gitignored.

# Required: your OpenAI API key.
OPENAI_API_KEY=sk-...

# Optional: shared password for the Streamlit app.
# If unset, the app is open access (use only for local dev).
APP_PASSWORD=change-me
